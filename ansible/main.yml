#!/usr/bin/env ansible-playbook
---
- name: Set up server
  hosts: all
  become: yes
  pre_tasks:
    - name: Update APT
      apt: update_cache=yes cache_valid_time=86400
  roles:
    - development
    - temperature_sensor
